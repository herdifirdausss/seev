openapi: 3.1.0
info:
    title: Seev Admin BFF API
    version: 1.0.0
    description: Operator-facing JSON/form contract at the Admin BFF edge.
servers: [{url: 'https://admin.example.invalid'}]
paths:
    /api/v1/admin/audit:
        get:
            operationId: adminListAuditV1
            x-see-owner: admin-bff
            x-see-audience: admin
            security: [{sessionCookie: []}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '403': {$ref: {description: Authenticated principal lacks permission., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/admin/adjustments:
        get:
            operationId: adminListAdjustmentsV1
            x-see-owner: admin-bff
            x-see-behavior-owner: ledger
            x-see-audience: admin
            security: [{sessionCookie: []}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '403': {$ref: {description: Authenticated principal lacks permission., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '502': {$ref: {description: A dependency is temporarily unavailable., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
        post:
            operationId: adminCreateAdjustmentV1
            x-see-owner: admin-bff
            x-see-behavior-owner: ledger
            x-see-audience: admin
            security: [{sessionCookie: []}]
            requestBody: {$ref: {required: true, content: {application/x-www-form-urlencoded: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'201': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '403': {$ref: {description: Authenticated principal lacks permission., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '409': {$ref: {description: Idempotency or state conflict., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/admin/adjustments/{id}:
        get:
            operationId: adminGetAdjustmentV1
            x-see-owner: admin-bff
            x-see-behavior-owner: ledger
            x-see-audience: admin
            security: [{sessionCookie: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '403': {$ref: {description: Authenticated principal lacks permission., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/admin/adjustments/approve:
        post:
            operationId: adminApproveAdjustmentV1
            x-see-owner: admin-bff
            x-see-behavior-owner: ledger
            x-see-audience: admin
            security: [{sessionCookie: []}]
            requestBody: {$ref: {required: true, content: {application/x-www-form-urlencoded: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '403': {$ref: {description: Authenticated principal lacks permission., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '409': {$ref: {description: Idempotency or state conflict., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/admin/adjustments/reject:
        post:
            operationId: adminRejectAdjustmentV1
            x-see-owner: admin-bff
            x-see-behavior-owner: ledger
            x-see-audience: admin
            security: [{sessionCookie: []}]
            requestBody: {$ref: {required: true, content: {application/x-www-form-urlencoded: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '403': {$ref: {description: Authenticated principal lacks permission., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '409': {$ref: {description: Idempotency or state conflict., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/admin/recon/batches:
        get:
            operationId: adminListReconBatchesV1
            x-see-owner: admin-bff
            x-see-behavior-owner: ledger
            x-see-audience: admin
            security: [{sessionCookie: []}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '403': {$ref: {description: Authenticated principal lacks permission., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
        post:
            operationId: adminUploadReconBatchV1
            x-see-owner: admin-bff
            x-see-behavior-owner: ledger
            x-see-audience: admin
            security: [{sessionCookie: []}]
            requestBody:
                required: true
                content: {multipart/form-data: {schema: {type: object, required: [file], additionalProperties: false, properties: {file: {type: string, format: binary}}}}}
            responses: {'201': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '403': {$ref: {description: Authenticated principal lacks permission., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '413': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/admin/ledger/fee-rules:
        get:
            operationId: adminListFeeRulesV1
            x-see-owner: admin-bff
            x-see-behavior-owner: ledger
            x-see-audience: admin
            security: [{sessionCookie: []}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '403': {$ref: {description: Authenticated principal lacks permission., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
        post:
            operationId: adminCreateFeeRuleV1
            x-see-owner: admin-bff
            x-see-behavior-owner: ledger
            x-see-audience: admin
            security: [{sessionCookie: []}]
            requestBody: {$ref: {required: true, content: {application/x-www-form-urlencoded: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'201': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '403': {$ref: {description: Authenticated principal lacks permission., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '409': {$ref: {description: Idempotency or state conflict., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/admin/ledger/fee-rules/{id}:
        put:
            operationId: adminUpdateFeeRuleV1
            x-see-owner: admin-bff
            x-see-behavior-owner: ledger
            x-see-audience: admin
            security: [{sessionCookie: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}]
            requestBody: {$ref: {required: true, content: {application/x-www-form-urlencoded: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '403': {$ref: {description: Authenticated principal lacks permission., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/admin/kyc/submissions:
        get:
            operationId: adminListKycSubmissionsV1
            x-see-owner: admin-bff
            x-see-behavior-owner: auth
            x-see-audience: admin
            security: [{sessionCookie: []}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '403': {$ref: {description: Authenticated principal lacks permission., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/admin/privacy/requests:
        get:
            operationId: adminListPrivacyRequestsV1
            x-see-owner: admin-bff
            x-see-behavior-owner: auth
            x-see-audience: admin
            security: [{sessionCookie: []}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '403': {$ref: {description: Authenticated principal lacks permission., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/admin/gateway/status:
        get:
            operationId: adminGetGatewayStatusV1
            x-see-owner: admin-bff
            x-see-behavior-owner: gateway
            x-see-audience: admin
            security: [{sessionCookie: []}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '403': {$ref: {description: Authenticated principal lacks permission., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '502': {$ref: {description: A dependency is temporarily unavailable., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
components:
    securitySchemes:
        sessionCookie: {$ref: {type: apiKey, in: cookie, name: seev_session}}
