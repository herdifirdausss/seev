openapi: 3.1.0
info:
    title: Seev public user API
    version: 1.0.0
    description: Stable v1 user contract for auth and gateway-owned product journeys.
servers:
    - {url: 'https://api.example.invalid', description: Gateway public edge}
    - {url: 'https://auth.example.invalid', description: Auth service public listener}
tags:
    - {name: auth, description: Authentication and user profile}
    - {name: ledger, description: User ledger operations}
    - {name: payments, description: Top-up and payout operations}
    - {name: notifications, description: User notification inbox}
paths:
    /api/v1/auth/register:
        post:
            operationId: authRegisterV1
            tags: [auth]
            servers: [{url: 'https://auth.example.invalid'}]
            x-see-owner: auth
            x-see-audience: public
            security: []
            requestBody: {$ref: {required: true, content: {application/json: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'201': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '409': {$ref: {description: Idempotency or state conflict., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '429': {$ref: {description: Rate limit exceeded., headers: {Retry-After: {schema: {type: integer, minimum: 1}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/auth/login:
        post:
            operationId: authLoginV1
            tags: [auth]
            servers: [{url: 'https://auth.example.invalid'}]
            x-see-owner: auth
            x-see-audience: public
            security: []
            requestBody: {$ref: {required: true, content: {application/json: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '429': {$ref: {description: Rate limit exceeded., headers: {Retry-After: {schema: {type: integer, minimum: 1}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/auth/refresh:
        post:
            operationId: authRefreshV1
            tags: [auth]
            servers: [{url: 'https://auth.example.invalid'}]
            x-see-owner: auth
            x-see-audience: public
            security: []
            requestBody: {$ref: {required: true, content: {application/json: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/users/me:
        get:
            operationId: authGetMeV1
            tags: [auth]
            servers: [{url: 'https://auth.example.invalid'}]
            x-see-owner: auth
            x-see-audience: public
            security: [{bearerAuth: []}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
        put:
            operationId: authUpdateMeV1
            tags: [auth]
            servers: [{url: 'https://auth.example.invalid'}]
            x-see-owner: auth
            x-see-audience: public
            security: [{bearerAuth: []}]
            requestBody: {$ref: {required: true, content: {application/json: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/users/me/kyc:
        get:
            operationId: authGetKycV1
            tags: [auth]
            x-see-owner: auth
            x-see-audience: public
            security: [{bearerAuth: []}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
        post:
            operationId: authSubmitKycV1
            tags: [auth]
            x-see-owner: auth
            x-see-audience: public
            security: [{bearerAuth: []}]
            requestBody: {$ref: {required: true, content: {application/json: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'202': {$ref: {description: Accepted for asynchronous processing., content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '409': {$ref: {description: Idempotency or state conflict., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/users/me/kyc/documents:
        post:
            operationId: authUploadKycDocumentV1
            tags: [auth]
            x-see-owner: auth
            x-see-audience: public
            security: [{bearerAuth: []}]
            requestBody:
                required: true
                content: {multipart/form-data: {schema: {type: object, required: [document], additionalProperties: false, properties: {document: {type: string, format: binary}, kind: {type: string, maxLength: 64}}}}}
            responses: {'201': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '413': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/users/me/privacy/exports:
        post:
            operationId: authCreatePrivacyExportV1
            tags: [auth]
            x-see-owner: auth
            x-see-audience: public
            security: [{bearerAuth: []}]
            requestBody: {$ref: {required: true, content: {application/json: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'202': {$ref: {description: Accepted for asynchronous processing., content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '409': {$ref: {description: Idempotency or state conflict., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/users/me/privacy/requests/{id}:
        get:
            operationId: authGetPrivacyExportStatusV1
            tags: [auth]
            x-see-owner: auth
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/users/me/privacy/exports/{id}/download:
        get:
            operationId: authDownloadPrivacyExportV1
            tags: [auth]
            x-see-owner: auth
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}]
            responses: {'200': {$ref: {description: Binary document., content: {application/octet-stream: {schema: {type: string, format: binary}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/users/me/privacy/closure:
        post:
            operationId: authCreatePrivacyClosureV1
            tags: [auth]
            x-see-owner: auth
            x-see-audience: public
            security: [{bearerAuth: []}]
            requestBody: {$ref: {required: true, content: {application/json: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'202': {$ref: {description: Accepted for asynchronous processing., content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '409': {$ref: {description: Idempotency or state conflict., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/users/me/privacy/closure/{id}:
        get:
            operationId: authGetPrivacyClosureStatusV1
            tags: [auth]
            x-see-owner: auth
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/topup:
        post:
            operationId: gatewayCreateTopupV1
            tags: [payments]
            x-see-owner: gateway
            x-see-behavior-owner: payin
            x-see-audience: public
            security: [{bearerAuth: []}]
            requestBody: {$ref: {required: true, content: {application/json: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'201': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '409': {$ref: {description: Idempotency or state conflict., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '422': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/topup/{id}:
        get:
            operationId: gatewayGetTopupV1
            tags: [payments]
            x-see-owner: gateway
            x-see-behavior-owner: payin
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/ledger/savings/products:
        get:
            operationId: gatewayListLedgerSavingsProductsV1
            tags: [savings]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '503': {$ref: {description: A dependency is temporarily unavailable., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/ledger/savings/enrollments:
        get:
            operationId: gatewayListLedgerSavingsEnrollmentsV1
            tags: [savings]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '503': {$ref: {description: A dependency is temporarily unavailable., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/ledger/savings/enrollments/{id}:
        get:
            operationId: gatewayGetLedgerSavingsEnrollmentV1
            tags: [savings]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/ledger/savings/enrollments/{id}/accruals:
        get:
            operationId: gatewayListLedgerSavingsAccrualsV1
            tags: [savings]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/ledger/savings/enrollments/{id}/periods:
        get:
            operationId: gatewayListLedgerSavingsPeriodsV1
            tags: [savings]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/payout:
        post:
            operationId: gatewayCreatePayoutV1
            tags: [payments]
            x-see-owner: gateway
            x-see-behavior-owner: payout
            x-see-audience: public
            security: [{bearerAuth: []}]
            requestBody: {$ref: {required: true, content: {application/json: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'201': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '409': {$ref: {description: Idempotency or state conflict., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '422': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/payout/{id}:
        get:
            operationId: gatewayGetPayoutV1
            tags: [payments]
            x-see-owner: gateway
            x-see-behavior-owner: payout
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/notifications:
        get:
            operationId: gatewayListNotificationsV1
            tags: [notifications]
            x-see-owner: gateway
            x-see-behavior-owner: notify
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{name: cursor, in: query, schema: {type: string, maxLength: 512}}, {name: before, in: query, description: Exclusive created_at timestamp for keyset pagination., schema: {type: string, format: date-time}}, {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 100}}, {name: unread, in: query, schema: {type: boolean}}, {name: category, in: query, schema: {type: string, enum: [money_movement, account, security, compliance, system]}}, {name: kind, in: query, schema: {type: string, maxLength: 64}}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/notifications/{id}:
        get:
            operationId: gatewayGetNotificationV1
            tags: [notifications]
            x-see-owner: gateway
            x-see-behavior-owner: notify
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/notifications/unread-count:
        get:
            operationId: gatewayGetNotificationUnreadCountV1
            tags: [notifications]
            x-see-owner: gateway
            x-see-behavior-owner: notify
            x-see-audience: public
            security: [{bearerAuth: []}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/notifications/{id}/read:
        post:
            operationId: gatewayMarkNotificationReadV1
            tags: [notifications]
            x-see-owner: gateway
            x-see-behavior-owner: notify
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}]
            responses: {'204': {$ref: {description: Operation completed without a response body.}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/notifications/read-all:
        post:
            operationId: gatewayMarkAllNotificationsReadV1
            tags: [notifications]
            x-see-owner: gateway
            x-see-behavior-owner: notify
            x-see-audience: public
            security: [{bearerAuth: []}]
            requestBody: {$ref: {required: true, content: {application/json: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'204': {$ref: {description: Operation completed without a response body.}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/notification-settings:
        get:
            operationId: gatewayGetNotificationSettingsV1
            tags: [notifications]
            x-see-owner: gateway
            x-see-behavior-owner: notify
            x-see-audience: public
            security: [{bearerAuth: []}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '503': {$ref: {description: A dependency is temporarily unavailable., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
        put:
            operationId: gatewayUpdateNotificationSettingsV1
            tags: [notifications]
            x-see-owner: gateway
            x-see-behavior-owner: notify
            x-see-audience: public
            security: [{bearerAuth: []}]
            requestBody: {$ref: {required: true, content: {application/json: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '409': {$ref: {description: Idempotency or state conflict., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '503': {$ref: {description: A dependency is temporarily unavailable., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/notification-preferences:
        get:
            operationId: gatewayGetNotificationPreferencesV1
            tags: [notifications]
            x-see-owner: gateway
            x-see-behavior-owner: notify
            x-see-audience: public
            security: [{bearerAuth: []}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '503': {$ref: {description: A dependency is temporarily unavailable., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
        put:
            operationId: gatewayUpdateNotificationPreferencesV1
            tags: [notifications]
            x-see-owner: gateway
            x-see-behavior-owner: notify
            x-see-audience: public
            security: [{bearerAuth: []}]
            requestBody: {$ref: {required: true, content: {application/json: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '409': {$ref: {description: Idempotency or state conflict., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '503': {$ref: {description: A dependency is temporarily unavailable., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/notification-devices:
        get:
            operationId: gatewayListNotificationDevicesV1
            tags: [notifications]
            x-see-owner: gateway
            x-see-behavior-owner: notify
            x-see-audience: public
            security: [{bearerAuth: []}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '503': {$ref: {description: A dependency is temporarily unavailable., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
        post:
            operationId: gatewayRegisterNotificationDeviceV1
            tags: [notifications]
            x-see-owner: gateway
            x-see-behavior-owner: notify
            x-see-audience: public
            security: [{bearerAuth: []}]
            requestBody: {$ref: {required: true, content: {application/json: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'201': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '409': {$ref: {description: Idempotency or state conflict., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '503': {$ref: {description: A dependency is temporarily unavailable., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/notification-devices/{id}:
        delete:
            operationId: gatewayRevokeNotificationDeviceV1
            tags: [notifications]
            x-see-owner: gateway
            x-see-behavior-owner: notify
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}]
            responses: {'204': {$ref: {description: Operation completed without a response body.}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '503': {$ref: {description: A dependency is temporarily unavailable., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/ledger/transactions:
        post:
            operationId: gatewayPostLedgerTransactionV1
            tags: [ledger]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            requestBody: {$ref: {required: true, content: {application/json: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'201': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '403': {$ref: {description: Authenticated principal lacks permission., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '409': {$ref: {description: Idempotency or state conflict., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '422': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '429': {$ref: {description: Rate limit exceeded., headers: {Retry-After: {schema: {type: integer, minimum: 1}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '503': {$ref: {description: A dependency is temporarily unavailable., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/ledger/transactions/{id}:
        get:
            operationId: gatewayGetLedgerTransactionV1
            tags: [ledger]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/ledger/accounts:
        get:
            operationId: gatewayListLedgerAccountsV1
            tags: [ledger]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{name: cursor, in: query, schema: {type: string, maxLength: 512}}, {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 100}}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/ledger/accounts/{id}/balance:
        get:
            operationId: gatewayGetLedgerBalanceV1
            tags: [ledger]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/ledger/accounts/{id}/entries:
        get:
            operationId: gatewayListLedgerEntriesV1
            tags: [ledger]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}, {name: cursor, in: query, schema: {type: string, maxLength: 512}}, {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 100}}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/ledger/accounts/{id}/statement:
        get:
            operationId: gatewayGetLedgerStatementV1
            tags: [ledger]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}, {name: format, in: query, schema: {type: string, enum: [json, csv]}}]
            responses: {'200': {$ref: {description: 'Account statement in JSON or CSV form, selected by the format query parameter.', content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}, text/csv: {schema: {type: string}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/ledger/accounts/pockets:
        post:
            operationId: gatewayCreateLedgerPocketV1
            tags: [ledger]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            requestBody: {$ref: {required: true, content: {application/json: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'201': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '409': {$ref: {description: Idempotency or state conflict., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/ledger/fees/quote:
        post:
            operationId: gatewayCreateLedgerFeeQuoteV1
            tags: [ledger]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            requestBody: {$ref: {required: true, content: {application/json: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'201': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '422': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/ledger/schedules:
        get:
            operationId: gatewayListLedgerSchedulesV1
            tags: [ledger]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
        post:
            operationId: gatewayCreateLedgerScheduleV1
            tags: [ledger]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            requestBody: {$ref: {required: true, content: {application/json: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'201': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/ledger/schedules/{id}/pause:
        post:
            operationId: gatewayPauseLedgerScheduleV1
            tags: [ledger]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}]
            responses: {'204': {$ref: {description: Operation completed without a response body.}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '409': {$ref: {description: Idempotency or state conflict., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/ledger/schedules/{id}/resume:
        post:
            operationId: gatewayResumeLedgerScheduleV1
            tags: [ledger]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}]
            responses: {'204': {$ref: {description: Operation completed without a response body.}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '409': {$ref: {description: Idempotency or state conflict., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/ledger/schedules/{id}/cancel:
        post:
            operationId: gatewayCancelLedgerScheduleV1
            tags: [ledger]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}]
            responses: {'204': {$ref: {description: Operation completed without a response body.}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '409': {$ref: {description: Idempotency or state conflict., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/ledger/schedules/{id}/confirm-fee-cap:
        post:
            operationId: gatewayConfirmLedgerScheduleFeeCapV1
            tags: [ledger]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}]
            requestBody: {$ref: {required: true, content: {application/json: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '409': {$ref: {description: Idempotency or state conflict., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/ledger/schedules/{id}/occurrences:
        get:
            operationId: gatewayListLedgerScheduleOccurrencesV1
            tags: [ledger]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}, {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 500}}, {name: offset, in: query, schema: {type: integer, minimum: 0}}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/ledger/schedules/{id}/occurrences/{occurrence_id}:
        get:
            operationId: gatewayGetLedgerScheduleOccurrenceV1
            tags: [ledger]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}, {name: occurrence_id, in: path, required: true, schema: {type: string, format: uuid}}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/scheduled-transactions/{id}/executions:
        get:
            operationId: gatewayListScheduledTransactionExecutionsV1
            tags: [ledger]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}, {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 500}}, {name: offset, in: query, schema: {type: integer, minimum: 0}}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/scheduled-transactions/{id}/executions/{execution_id}:
        get:
            operationId: gatewayGetScheduledTransactionExecutionV1
            tags: [ledger]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}, {name: execution_id, in: path, required: true, schema: {type: string, format: uuid}}]
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
    /api/v1/scheduled-transactions/{id}/confirm-fee-cap:
        post:
            operationId: gatewayConfirmScheduledTransactionFeeCapV1
            tags: [ledger]
            x-see-owner: gateway
            x-see-behavior-owner: ledger
            x-see-audience: public
            security: [{bearerAuth: []}]
            parameters: [{$ref: {name: id, in: path, required: true, schema: {$ref: {type: string, format: uuid, examples: ['00000000-0000-7000-8000-000000000001']}}}}]
            requestBody: {$ref: {required: true, content: {application/json: {schema: {$ref: {type: object, additionalProperties: true, description: Operation-specific request schemas tighten this shape; this shared fallback is not a permission to accept unknown fields.}}}}}}
            responses: {'200': {$ref: {description: Successful JSON response., headers: {X-Request-ID: {description: Correlation identifier echoed by the server., schema: {type: string, minLength: 1, maxLength: 128}}}, content: {application/json: {schema: {$ref: {type: object, required: [success, data], additionalProperties: false, properties: {success: {const: true}, data: {}, meta: {type: object, additionalProperties: true}}}}}}}}, '400': {$ref: {description: 'Invalid request, including unknown fields.', content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '401': {$ref: {description: Authentication is required or invalid., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '404': {$ref: {description: Resource was not found., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}, '409': {$ref: {description: Idempotency or state conflict., content: {application/json: {schema: {$ref: {type: object, required: [success, error], additionalProperties: false, properties: {success: {const: false}, error: {$ref: {type: object, required: [code, message], additionalProperties: false, properties: {code: {type: string, pattern: '^[A-Z][A-Z0-9_]{2,63}$'}, message: {type: string, minLength: 1, maxLength: 512}, request_id: {type: string, maxLength: 128, description: "Optional, additive (Plan 57 §6.7) — duplicates the X-Request-ID response header inside the error body itself, for programmatic B2B clients that don't always have header access. Every consumer of the pre-existing ErrorEnvelope/Error shape keeps working unchanged since this field is optional."}, details: {type: object, additionalProperties: true}}}}}}}}}}}}
components:
    securitySchemes:
        bearerAuth: {$ref: {type: http, scheme: bearer, bearerFormat: JWT}}
