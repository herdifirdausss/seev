# K0 service-probe evidence

Store bounded, redacted liveness/readiness probe output here. Probe commands
must use synthetic local services and must not include tokens, certificates,
private keys, or request payloads.
